diff --git a/components/omnibox/browser/BUILD.gn b/components/omnibox/browser/BUILD.gn
index 2e8dee293107bd35fe8e8b11561e67bb31074ea7..7cc80d62f72f678e802401174df6e34a14eeba93 100644
--- a/components/omnibox/browser/BUILD.gn
+++ b/components/omnibox/browser/BUILD.gn
@@ -262,6 +262,7 @@ jumbo_static_library("browser") {
   if ((!is_android || enable_vr) && !is_ios) {
     deps += [ ":vector_icons" ]
   }
+  if (brave_chromium_build) {  deps += [ "//brave/components/omnibox/browser" ] }
 
   if (is_android) {
     sources += [
@@ -410,7 +411,15 @@ jumbo_static_library("test_support") {
 }
 
 bundle_data("unit_tests_bundle_data") {
-  visibility = [ ":unit_tests" ]
+  visibility = [
+    ":unit_tests",
+  ]
+
+  # For brave test.
+  visibility += [
+    "//brave/test:brave_unit_tests",
+  ]
+
   testonly = true
   sources = [
     "//components/test/data/omnibox/Shortcuts.no_fill_into_edit.sql",
